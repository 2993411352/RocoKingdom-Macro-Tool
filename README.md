# 洛克王国：世界 — 自动化宏工具

基于 Python 的《洛克王国：世界》全自动挂机方案。支持自定义动作场景与 **视觉防卡死机制**（自动解除果冻状态、自动关闭干扰弹窗），实现真正的无干预后台挂机。

## 🎯 核心特性

- 🎮 **全局热键控制** — `Ctrl+Space` / `Alt+Space` 一键启动/停止
- 🪟 **窗口焦点检测** — 游戏失焦自动暂停，切回后自动恢复动作
- 🔄 **智能定时重置** — 自带 5 分钟安全重置机制，补发全队精灵
- 👁️ **图像视觉纠错** — 受到控制（果冻）自动按 X 解除；遭遇弹窗遮挡自动定位关闭
- 📝 **极简配置中心** — `config.py` 和 `presets.py` 界面分离，可根据需求极速自定义新动作

## 🚀 下载与使用

### 1. 快速使用 (推荐)
1. 在 [Releases](https://github.com/2993411352/RocoKingdom-Macro-Tool/releases) 页面下载最新的 `RocoKingdom-Macro-Release.zip`。
2. 解压后，保持 `templates/` 文件夹与可执行程序在同级目录。
3. **以管理员身份运行** `RocoKingdom-Macro.exe`。
4. 终端会弹出可用预设菜单，输入编号启动对应的动作序列。

### 2. 开发者源码运行
```bash
git clone https://github.com/2993411352/RocoKingdom-Macro-Tool.git
pip install -r requirements.txt
python main.py
```

## 🎮 当前可用预设 (Presets)

启动后会弹出交互式菜单供选择：

| 编号 | 预设名称 | 执行逻辑 |
|------|------|------|
| 1 | 终端自定义 | 直接在控制台手动输入你的专属动作序列 |
| 2 | 主座设置 | 扔出2~6号精灵后骑乘1号，每20秒按R防掉线 |
| 3 | 纯鞠躬小号设置 | 扔出1~6号精灵后，持续循环: Tab→鞠躬→Esc |
| 4 | 单人采集设置 | 扔出2~6号精灵，循环: Tab→鞠躬→Esc→跳跃→ RDown → RUp |

*(如有需要，您可以使用记事本打开并编辑 `presets.py` 任意增减修改这些循环逻辑)*


## ⌨️ 动作指令字典

在自定义专属于你的循环时，只需把指令用**空格**隔开即可，目前支持以下几类动作：

**【鼠标操控】**
- `LMouseDown` / `LMouseUp` ：按下 / 松开 鼠标左键
- `WheelUp` / `WheelDown` ：向上滚 / 向下滚 鼠标滚轮

**【键盘敲击】**
- **长按 / 抬起**：支持给任意字母或按键加上状态，例如 `QDown` (按下Q)、`XUp` (松开X)、`SpaceDown` (按下空格)。
- **真实敲击模拟**：直接写 `Esc` 或 `Tab`，程序会自动模拟包含天然微小延迟按下并松开的物理反馈。

**【游戏快捷功能】**
- `C1` ~ `C6` ：智能热键，替你精确按下数字 1~6 切换对应的精灵。
- `Bow` ：一键执行“脱战鞠躬”的标准化交互动作序列。

**【流程控制】**
- **等待时间**：直接输入数字（例如 `800` 或 `2000`），代表在这里停顿休息 800毫秒 / 2秒。给人物一点做动作的时间！
- `Sound` ：内置发声器，播放一声 `咚` 的提示音，适合用来给循环画句号。


## 📂 极简项目结构

```text
RocoKingdom-Macro/
├── core/                  
├── debug/                
├── templates/             
├── config.py              
├── presets.py            
└── main.py             
```

## 🚫 免责声明

> [!WARNING]  
> **本工具仅供编程学习与自动化开源技术交流，切勿用于破坏特定游戏的公平环境。**

1. 将本工具用于实际游戏操作面临一定风险，极可能会违反使用条款（EULA），导致账号被限权甚至封禁，**请使用者自行承担一切相关后果**。
2. 此项目为纯免费开源性质，严禁任何人将此工具包装后进行商业售卖或代练牟利。
3. 所有关于《洛克王国》的图标与美术资源等知识产权皆属于其原合法发行商（腾讯相关工作室）所有。
